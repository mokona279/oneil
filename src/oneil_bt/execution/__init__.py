"""체결 레이어 (계획서 §3.5, §6.2).

주문 객체(`Order`), 거래비용(`CostModel`), 일봉 기반 결정론적 체결(`DailyBarFillModel`)을
담는다. 판정(규칙)과 체결(가격·비용)을 분리해, 룩어헤드 없는 §6.1 타이밍 계약을
체결 단계에서 물리적으로 강제한다.

Phase 4A는 진입(1차 돌파)·피라미딩(2·3차) 체결과 비용, Phase 4B는 청산 체결(`fill_exit`,
손절·60MA·시장방어)까지 담는다.
"""
